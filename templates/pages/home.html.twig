{% extends 'base.html.twig'%}

{% block title 'Homepage' %}

{% block body %}
  <div class="jumbotron text-center">
    <h1>Agence Blablabla</h1>
    <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Dolorum, ipsam laudantium cumque officia nemo, eveniet accusantium velit eius labore numquam veritatis libero incidunt culpa sint, explicabo atque inventore autem magnam.</p>
  </div>

  <div class="container">
    <h2>Les derniers biens</h2>
    <div class="row flex">
      {% for property in properties %}
        <div class="col-3">
          <div class="card">
            <div class="card-body">
              <div class="card-title">
                <h5><a href="{{ path('properties.show', { id: property.id, slug: property.slug }) }}">{{ property.title }}</a></h5>
                <p class="card-text">{{  property.city }} ({{ property.postalCode }})</p>
                <div class="text-primary">{{ property.formattedPrice }} €</div>
              </div>
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  </div>
{% endblock %}